/**
 * Blog Configuration Module
 *
 * This module provides all configuration constants and helper functions
 * for the blog package. The configuration is split into focused files:
 *
 * - `constants.ts` - Path patterns, base paths, and file extensions
 * - `helpers.ts` - Helper functions for configuration
 * - `paths.ts` - Main entry point that re-exports everything
 * - `mdx-components.tsx` - Default MDX component mappings
 */

export * from "./paths";
export * from "./mdx-components";
